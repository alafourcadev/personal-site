// La Forja evaluation engine — pure TypeScript, no DOM, no React, no Astro imports.
// Full evaluate()/checkConnection() surface lands in R1-B/R1-C. This stub exists
// only to unblock the Strict TDD harness gate (R1-A).

export const ENGINE_VERSION = '0.0.0-r1a'
