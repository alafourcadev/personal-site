node_modules
dist
.vite
*.local
.astro
!public/
.vercel

# Credentials. `.env.example` is the tracked template; real values never are.
.env
.env.*
!.env.example

# Drafts (en Obsidian vault)
content-drafts/

# Agent tooling cache
.atl/

# Los agentes SÍ se versionan: son parte del proyecto, no configuración local.
# (el gitignore global ignora .claude entero)
!.claude/
.claude/*
!.claude/agents/
